node_modules/
__pycache__/
.DS_Store
*.pyc
# Client test data — never commit to repo
pwin-bid-execution/test-data/
# Obsidian vault config — personal, not committed
.obsidian/
HOME.md

# Competitive intel analysis exports — local reports, not source
pwin-competitive-intel/analysis/

# Local Claude Code settings — per-user, not committed
.claude/

# Playwright MCP scratch logs and page snapshots
.playwright-mcp/

# Ad-hoc UI screenshots captured at the repo root
/qualify-*.png
/cfo-*.png

# Runtime logs from competitive intel agents — generated, not source
pwin-competitive-intel/logs/
pwin-competitive-intel/agent/*.log

# Skill test outputs — local artefacts, not source
pwin-platform/test/skill-tests/results/

# Canonical buyer alias review exports — derived from canonical_buyers + alias glossary, not source
pwin-competitive-intel/canonical-buyer-aliases-review*.csv

# Built skill archives — derived from the master source folders, not source
pwin-platform/skills/agent2-market-competitive/master/*.skill
pwin-platform/skills/agent2-market-competitive/master/*.zip

# Temp download files from seed scripts (e.g. GIAS academy trust spreadsheet)
pwin-platform/knowledge/.tmp_*.xlsx
pwin-competitive-intel/db/.tmp.drive*/

# Brainstorming session files (visual companion)
.superpowers/
